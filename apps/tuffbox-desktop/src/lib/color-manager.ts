/** Re-export — implementation lives in `@tuffbox/quest-lib`. */
export {
  parseColorSegments,
  getColorAtPosition,
  searchColorHits,
  aggregateColors,
  recolorAtPosition,
  recolorAllOccurrences,
  type ColorHit,
  type ColorAggregation,
  type ColorSearchOptions,
} from "@tuffbox/quest-lib";

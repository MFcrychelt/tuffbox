/** Re-export — implementation lives in `@tuffbox/quest-lib` (full parse + serialize). */
export {
  serializeSnbt,
  parseSnbt,
  isSnbtTypedArray,
  asSnbtList,
  type SnbtArrayType,
  type SnbtTypedArray,
  type SnbtValue,
} from "@tuffbox/quest-lib";

/** Re-export — implementation lives in `@tuffbox/quest-lib`. */
export {
  serializeSnbt,
  parseSnbt,
  isSnbtTypedArray,
  asSnbtList,
  type SnbtArrayType,
  type SnbtTypedArray,
  type SnbtValue,
} from "@tuffbox/quest-lib";

/** Re-export — implementation lives in `@tuffbox/quest-lib`. */
export {
  createSelectionState,
  selectSingle,
  toggleSelect,
  addToSelection,
  selectAll,
  selectRange,
  selectInRect,
  clearSelection,
  type SelectionState,
} from "@tuffbox/quest-lib";
